import { ComingSoon } from '@/components/ComingSoon'

export default function BudgetsPage() {
  return (
    <ComingSoon
      title="Budgets"
      phase="Phase 10"
      summary="Per-category limits with a month-end projection based on run-rate and recurring charges still to land. Advisory only — budgets inform, they never block or nag."
    />
  )
}
