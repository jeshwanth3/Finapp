import { ComingSoon } from '@/components/ComingSoon'

export default function DebtPage() {
  return (
    <ComingSoon
      title="Debt"
      phase="Phase 4 — in progress"
      summary="Balance, statement balance, minimum due, due date and APR for every card, rendered as a calendar rather than a table. The question is what's coming and when, not what the list is."
    />
  )
}
